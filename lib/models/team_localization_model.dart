import 'package:flutter/material.dart';

class TeamLocalizationModel {
  final Map<Locale, List<String>> localizedTeamList;

  const TeamLocalizationModel({
    required this.localizedTeamList,
  });
}
